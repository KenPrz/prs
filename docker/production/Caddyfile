# Caddy terminates TLS (automatic Let's Encrypt) and proxies to the app.
# APP_DOMAIN comes from the environment (set in compose.prod.yaml / .env).
{$APP_DOMAIN} {
	encode gzip
	reverse_proxy app:8080
}
